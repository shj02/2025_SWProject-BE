# src/main/resources/application.yml

server:
  port: 8080

spring:
  # --- 데이터 소스 (MySQL 연결) 설정 ---
  datasource:
    # DB 이름: mongletrip_db (확인 완료)
    url: jdbc:mysql://localhost:3306/mongletrip_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8

    # 사용자 이름은 root 계정 사용
    username: root

    # ⚠️ 이곳을 당신이_정한_비밀번호 로 변경해야 합니다!
    password: 'hyeji2628@'
    driver-class-name: com.mysql.cj.jdbc.Driver

  # --- JPA (Hibernate) 설정 ---
  jpa:
    hibernate:
      # update: 서버 시작 시 엔티티를 보고 DB 테이블을 생성/업데이트합니다.
      ddl-auto: update
    properties:
      # 실행되는 SQL 쿼리를 콘솔에 표시합니다.
      hibernate.show_sql: true
      # SQL 쿼리를 보기 좋게 포맷합니다.
      hibernate.format_sql: true

  # --- 소셜 로그인 설정 ---
  spring:
    security:
      oauth2:
        client:
          registration:
            # 카카오 설정
            kakao:
              client-id: '4c3d625552d7070da8a55f3a11289f2b'  # 🚨 발급받은 REST API 키
              client-secret: 'PrPSHzpe1ozhiz0jDUBcHVerk5uONcwR' # 🚨 발급받은 Client Secret (활성화한 경우)
              client-authentication-method: post
              authorization-grant-type: authorization_code
              redirect-uri: 'http://mongle.com/login/kakao' # 🚨 예: http://localhost:8080/login/oauth2/code/kakao
              client-name: Kakao
            # 네이버 설정
            naver:
              client-id: 'RIAvMCtteoDQApL9dg0s' # 🚨 발급받은 클라이언트 ID
              client-secret: 'lvt2hg4m1D' # 🚨 발급받은 클라이언트 Secret
              client-authentication-method: client_secret_post
              authorization-grant-type: authorization_code
              redirect-uri: 'http://mongle.com/login/naver' # 🚨 예: http://localhost:8080/login/oauth2/code/naver
              client-name: Naver

          provider:
            kakao:
              authorization-uri: https://kauth.kakao.com/oauth/authorize
              token-uri: https://kauth.kakao.com/oauth/token
              user-info-uri: https://kapi.kakao.com/v2/user/me
              user-name-attribute: id
            naver:
              authorization-uri: https://nid.naver.com/oauth2.0/authorize
              token-uri: https://nid.naver.com/oauth2.0/token
              user-info-uri: https://openapi.naver.com/v1/nid/me
              user-name-attribute: response